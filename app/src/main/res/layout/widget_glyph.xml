<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="6dp">

    <!--
        Exactly one glyph, rendered full-size. A StackView/ListView is not used
        on purpose: StackView always draws its items as a scaled-down 3D deck
        with the next card peeking out behind, which shrank the glyph and let
        two glyphs show at once.
    -->
    <ImageView
        android:id="@+id/widget_glyph_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter" />

    <!-- Edge chevrons page to the previous/next glyph; hidden when the
         widget shows a single glyph. Full-height strips for easy tapping. -->
    <TextView
        android:id="@+id/widget_prev"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start|center_vertical"
        android:gravity="center"
        android:paddingHorizontal="8dp"
        android:text="‹"
        android:textColor="#666666"
        android:textSize="22sp"
        android:contentDescription="@string/widget_prev"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widget_next"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end|center_vertical"
        android:gravity="center"
        android:paddingHorizontal="8dp"
        android:text="›"
        android:textColor="#666666"
        android:textSize="22sp"
        android:contentDescription="@string/widget_next"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/widget_empty"
        android:textColor="#777777"
        android:textSize="13sp" />
</FrameLayout>
